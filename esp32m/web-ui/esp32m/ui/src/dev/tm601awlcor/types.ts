export const Name = 'TM601AWLCOR';

export interface IProps {
  name: string;
  title?: string;
}

export type TState = Array<number>;
export type TConfig = { reg1: number; reg2: number; reg3: number; };
